--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -782,6 +782,7 @@ if(CURL_USE_OPENSSL)
 
   if(HAVE_BORINGSSL)
     set(_openssl "BoringSSL")
+    add_definitions(-DBORINGSSL_PREFIX=BSSL)
   elseif(HAVE_AWSLC)
     set(_openssl "AWS-LC")
   elseif(HAVE_LIBRESSL)
